package util

const (
	GeneralPrompt        = "You are an expert at creating a git commit message for a set of changes. Here is a diff of changes we need a commit message for: "
	GEMINI_DEFAULT_MODEL = "models/gemini-flash-latest"
)
